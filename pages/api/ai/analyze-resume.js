// Use the "Next.js API Route" code from the backend artifact
import { createAnalysisPrompt } from '../../../utils/claude-api';

export default async function handler(req, res) {
  // ... (code from backend artifact)
}
