// Extract the Claude API utility functions from the backend code
export const createAnalysisPrompt = (resumeData, jobDesc, preferences) => {
  // ... function code
};

export const callClaudeAPI = async (prompt, maxTokens = 4000) => {
  // ... function code  
};
